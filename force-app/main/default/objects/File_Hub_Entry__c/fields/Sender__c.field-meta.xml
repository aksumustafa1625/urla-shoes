<?xml version="1.0" encoding="UTF-8"?>
<CustomField xmlns="http://soap.sforce.com/2006/04/metadata">
    <fullName>Sender__c</fullName>
    <deleteConstraint>SetNull</deleteConstraint>
    <description>User who uploaded the file. Defaults to the running user but stored explicitly so ownership transfers (e.g. employee leaves) don't break the audit trail.</description>
    <label>Sender</label>
    <referenceTo>User</referenceTo>
    <relationshipLabel>Files Sent</relationshipLabel>
    <relationshipName>File_Hub_Entries_Sent</relationshipName>
    <required>false</required>
    <trackHistory>false</trackHistory>
    <type>Lookup</type>
</CustomField>
